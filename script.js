
    let a=10;
    let b=20;
    let c=25;
    if(a>b && a>c){
        let msg=`${a} is greatest number`;
        console.log(msg);
    }
    else if(b>a && b>c){
        let msg=`${b} is greatest number`;
        console.log(msg);
    }
    else{
        let msg=`${c} is greatest number`;
        console.log(msg);
    }